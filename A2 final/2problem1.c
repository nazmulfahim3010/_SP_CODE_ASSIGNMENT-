#include<stdio.h>
int main()
{int a;
scanf("%d",&a);
    if (a==0)
    {

        printf("zero");
        
    }
    else 
    {
        printf("not zero");
    }
    return 0;
}/*Take an integer input and check whether the number is zero or not. If it is zero, print zero in the output, otherwise print nothing*/