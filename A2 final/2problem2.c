#include<stdio.h>
int main()
{int a;
scanf("%d",&a);
    if(0<a)
    {
        printf("positive");
    }
    else if (a<0)
    {
        printf("negative");
    }
    return 0;
}/*Take an integer input and check whether the number is negative or non-negative.*/